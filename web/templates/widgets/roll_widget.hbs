<div class="roll-widget">
    <h4 class="roll-title-header">{{t "Roll {count} {count, plural, one {die} other {dice}} with {range} sides" }}</h4>
    <div class="roll-box">
        <ul class="roll-results-list"></ul>
        <button class="roll-button">{{t "Roll" }}</button>
    </div>
</div>
